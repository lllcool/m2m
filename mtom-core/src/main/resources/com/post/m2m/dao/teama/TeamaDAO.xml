<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.post.m2m.dao.teama.TeamaDAO">


    <sql id="teamaColumns">
        ${alias}.id,
        ${alias}.team_name as teamName
    </sql>


    <select id="findById" resultType="TeamaPO">
        select
            <include refid="teamaColumns"><property name="alias" value="t"/></include>
        from teama t
        <where>
            and t.id = #{arg0}
        </where>
        limit 1
    </select>

    <select id="exist" resultType="boolean">
        select count(1) from teama
        <where>
            and id = #{arg0}
        </where>
    </select>

    <insert id="_save" keyProperty="id" parameterType="TeamaPO">
        insert into teama(
        id,
        team_name
        ) VALUES (
        #{id,jdbcType=VARCHAR},
        #{teamName,jdbcType=VARCHAR}
        )
    </insert>


    <update id="_update" parameterType="TeamaPO">
        update teama set
            team_name=#{teamName,jdbcType=VARCHAR}
        where id=#{id,jdbcType=VARCHAR}
    </update>

    <delete id="delete">
        delete from teama
        where id=#{arg0,jdbcType=VARCHAR}
    </delete>


    <sql id="queryCondition">
    </sql>

    <sql id="orderCondition">
        order by
            t.id desc
    </sql>

    <select id="findCountByQuery" parameterType="TeamaQO" resultType="int">
        select count(1) from teama t
        <where>
        <include refid="queryCondition"/>
        </where>
    </select>

    <select id="findListByQuery" parameterType="TeamaQO" resultType="TeamaListVO">
        select
            <include refid="teamaColumns"><property name="alias" value="t"/></include>
        from teama t
        <where>
        <include refid="queryCondition"/>
        </where>
        <include refid="orderCondition"/>
        limit #{startIndex},#{pageSize}
    </select>

    <select id="findOptions" parameterType="OptionQO" resultType="OptionVO">
        select
            t.id as `key`,
            t.team_name as `value`
        from teama t
        <where>
            <if test="lastKey != null">
                and t.id > #{lastKey}
            </if>
            <if test="matchValue != null and matchValue != '' ">
                and t.team_name = #{matchValue}
            </if>
        </where>
        order by t.id asc
        limit #{limit}
    </select>



    <!-- 以上是自动生成的代码，尽量不要手动修改，新的sql请写在本行注释以下区域 -->


</mapper>
