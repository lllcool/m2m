<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.post.m2m.dao.stu_ref_teach.StuRefTeachDAO">


    <sql id="stuRefTeachColumns">
        ${alias}.id,
        ${alias}.stu_id as stuId,
        ${alias}.teacher_id as teacherId
    </sql>


    <select id="findById" resultType="StuRefTeachPO">
        select
            <include refid="stuRefTeachColumns"><property name="alias" value="t"/></include>
        from stu_ref_teach t
        <where>
            and t.id = #{arg0}
        </where>
        limit 1
    </select>

    <select id="exist" resultType="boolean">
        select count(1) from stu_ref_teach
        <where>
            and id = #{arg0}
        </where>
    </select>

    <insert id="_save" keyProperty="id" parameterType="StuRefTeachPO">
        insert into stu_ref_teach(
        id,
        stu_id,
        teacher_id
        ) VALUES (
        #{id,jdbcType=VARCHAR},
        #{stuId,jdbcType=VARCHAR},
        #{teacherId,jdbcType=VARCHAR}
        )
    </insert>


    <update id="_update" parameterType="StuRefTeachPO">
        update stu_ref_teach set
            stu_id=#{stuId,jdbcType=VARCHAR},
            teacher_id=#{teacherId,jdbcType=VARCHAR}
        where id=#{id,jdbcType=VARCHAR}
    </update>

    <delete id="delete">
        delete from stu_ref_teach
        where id=#{arg0,jdbcType=VARCHAR}
    </delete>


    <sql id="queryCondition">
        <if test="stuId != null and stuId != '' ">
            and t.stu_id = #{stuId}
        </if>
        <if test="teacherId != null and teacherId != '' ">
            and t.teacher_id = #{teacherId}
        </if>
        <bind name="stu_name_con_ex" value="stuName != null and stuName != '' " />
        <if test="stu_name_con_ex ">
            and exists(
                select 1 from student e1
                where e1.id = t.stu_id
            <if test="stu_name_con_ex">
                <bind name="stuName_pattern" value="'%' + stuName + '%'" />
                and e1.`name` like #{stuName_pattern}
            </if>
            )
        </if>
        <bind name="tea_name_con_ex" value="teaName != null and teaName != '' " />
        <bind name="tea_subject_con_ex" value="teaSubject != null and teaSubject != '' " />
        <if test="tea_name_con_ex or tea_subject_con_ex ">
            and exists(
                select 1 from teacher e2
                where e2.id = t.teacher_id
            <if test="tea_name_con_ex">
                <bind name="teaName_pattern" value="'%' + teaName + '%'" />
                and e2.teacher_name like #{teaName_pattern}
            </if>
            <if test="tea_subject_con_ex">
                and e2.`subject` = #{teaSubject}
            </if>
            )
        </if>
    </sql>

    <sql id="orderCondition">
        order by
            t.id desc
    </sql>

    <select id="findCountByQuery" parameterType="StuRefTeachQO" resultType="int">
        select count(1) from stu_ref_teach t
        <where>
        <include refid="queryCondition"/>
        </where>
    </select>

    <select id="findListByQuery" parameterType="StuRefTeachQO" resultType="StuRefTeachListVO">
        select
            <include refid="stuRefTeachColumns"><property name="alias" value="t"/></include>
            ,c1.`name` as stuName
            ,c2.teacher_name as teaName
            ,c2.`subject` as teaSubject
        from stu_ref_teach t
        left outer join student c1
            on c1.id = t.stu_id
        left outer join teacher c2
            on c2.id = t.teacher_id
        <where>
        <include refid="queryCondition"/>
        </where>
        <include refid="orderCondition"/>
        limit #{startIndex},#{pageSize}
    </select>

    <select id="getCountByStuId" parameterType="String" resultType="int">
        select count(1)
        from stu_ref_teach t
        where
            t.stu_id=#{arg0}
    </select>

    <select id="getCountByTeacherId" parameterType="String" resultType="int">
        select count(1)
        from stu_ref_teach t
        where
            t.teacher_id=#{arg0}
    </select>



    <!-- 以上是自动生成的代码，尽量不要手动修改，新的sql请写在本行注释以下区域 -->


</mapper>
